# Migrated to Bun 1.4 - see package.json and src/
# Python dependencies are now minimal (history bridge uses python3-cups via apt, not pip)
# Kept for legacy pytest fallback; main app now runs on Bun.serve + Hono
