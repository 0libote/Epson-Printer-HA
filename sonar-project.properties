# Bun 1.4 migration - Sonar config
sonar.projectKey=0libote_Epson-Printer-HA
sonar.organization=0libote
# Exclude legacy Python app (kept for fallback) and build artifacts from duplication/cpd
sonar.exclusions=app/**,scan-bridge/install_bundle.py,data/**,public/**,tests/**,Dockerfile,scan-bridge/Dockerfile,**/Dockerfile,.github/**
sonar.cpd.exclusions=**/*
# Coverage exclusions for generated frontend bundle
sonar.coverage.exclusions=src/frontend/**,public/**
# Allow mutable let for test harness (APP_DIR etc. need runtime reconfiguration)
sonar.issue.enforce.multicriteria=S6471,S6470
